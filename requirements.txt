# PDF and ebook extraction
PyMuPDF>=1.23.0
ebooklib>=0.18
python-docx>=1.1.0

# OCR for scanned PDFs (requires tesseract-ocr system package)
ocrmypdf>=16.0.0

# LLM inference — local via Ollama, or cloud APIs
ollama>=0.3.0
anthropic>=0.39.0       # optional: for Claude backend
openai>=1.50.0          # optional: for Perplexity backend (OpenAI-compat)

# Graph databases
neo4j>=5.0.0
networkx>=3.0

# Web visualization server
flask>=3.0.0
flask-cors>=4.0.0

# Utilities
pyyaml>=6.0
tqdm>=4.65.0
numpy>=1.24.0
scikit-learn>=1.3.0
